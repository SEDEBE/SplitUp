<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.SplitUp"
        android:supportsRtl="true">

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.GroupListActivity"    android:exported="false"/>
        <activity android:name=".ui.GroupDetailActivity"  android:exported="false"/>
        <activity android:name=".ui.CreateExpenseActivity" android:exported="false"/>

    </application>
</manifest>
